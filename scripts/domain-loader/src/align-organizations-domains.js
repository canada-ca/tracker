const {
  checkClaimCount,
  checkDomain,
  checkOrganization,
  createClaim,
  createDomain,
  createOrganization,
  slugify,
} = require('./helpers')

const alignOrganizationsDomains = async () => {}

module.exports = {
  alignOrganizationsDomains,
}
