const {
  checkClaimCount,
  checkDomain,
  checkOrganization,
  createClaim,
  createDomain,
  createOrganization,
  slugify,
} = require('./helpers')

const alignOrganizationsDomains = () => {}

module.exports = {
  alignOrganizationsDomains,
}
