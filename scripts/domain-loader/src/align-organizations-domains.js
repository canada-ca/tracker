const {
  checkClaimCount,
  checkDomain,
  checkOrganization,
  createClaim,
  createDomain,
  createOrganization,
  slugify,
} = require('./src')

const alignOrganizationsDomains = () => {}

module.exports = {
  alignOrganizationsDomains,
}
