apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: https-scanner # The name of the app
  namespace: default # The namespace the app will use
spec:
  template:
    spec:
      containers:
        - image: gcr.io/track-compliance/https # The URL to the image of the app