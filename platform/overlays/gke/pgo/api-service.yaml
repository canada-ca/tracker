apiVersion: v1
kind: Service
metadata:
  name: postgres-operator
  namespace: api
spec:
  type: ClusterIP
  ports:
  - port: tcp-8080
    protocol: TCP
    targetPort: tcp-8080
  selector:
    name: postgres-operator
