const { findMyOrganizations } = require('./find-my-organizations')
const { findOrganizationBySlug } = require('./find-organization-by-slug')

module.exports = {
  findMyOrganizations,
  findOrganizationBySlug,
}
