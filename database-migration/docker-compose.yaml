services:
  database-migration:
    image: 'northamerica-northeast1-docker.pkg.dev/track-compliance/tracker/database-migration:dirty-${TAG}'
    build: ./
    env_file:
      - .env
    network_mode: "host"
