version: "3.7"
services:
  postgres:
    image: postgres:alpine
    env_file:
      - postgres.env
    expose:
      - "5432"
  api:
    image: gcr.io/track-compliance/api
    restart: always
    env_file:
      - api.env
    volumes:
      - ./:/app
    ports:
      - "5000:5000"
    depends_on:
      - postgres
      - migration
  migration:
    image: gcr.io/track-compliance/api
    command: pipenv run db-upgrade
    env_file:
      - api.env
    volumes:
      - ./:/app
    expose:
      - "5000"
    depends_on:
      - postgres

volumes:
  driver: {}
