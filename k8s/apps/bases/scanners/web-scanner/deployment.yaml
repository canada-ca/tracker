apiVersion: apps/v1
kind: Deployment
metadata:
  name: webtls-scanner
  namespace: scanners
spec:
  selector:
    matchLabels:
      app: web-scanner
  strategy: {}
  template:
    metadata:
      labels:
        app: web-scanner
    spec:
      containers:
        - name: web-scanner
          image: gcr.io/track-compliance/web-scanner:master-ad1b14d-1637090396 # {"$imagepolicy": "flux-system:web-scanner"}
          env:
            - name: PYTHONWARNINGS
              value: ignore
            - name: PUBLISH_TO
              value: domains
            - name: SUBSCRIBE_TO
              value: domains.*
            - name: QUEUE_GROUP
              value: webscanner
            - name: NATS_SERVERS
              value: nats://nats.pubsub:4222
            - name: NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
          resources: {}
status: {}
